import React from 'react';
import Search from '../molecules/Search';

const Header = () => {
  return (
    <header>
      <Search onSearch={() => {}} />
    </header>
  );
};

export default Header;
